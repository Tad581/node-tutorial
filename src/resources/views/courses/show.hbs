<div class="mt-4">
  <div class="row">
    <div class="col-lg-3">
      <button>Learn</button>
    </div>
    <div class="col-lg-9">
      <h2>{{course.name}}</h2>
      <img src="{{course.thumbnail}}" alt="" class="detail-course-thumbnail" />
      <p>{{course.description}}</p>
    </div>
  </div>
</div>
